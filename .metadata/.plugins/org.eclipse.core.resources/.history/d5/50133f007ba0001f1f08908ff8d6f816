<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BloodBank List</title>
</head>
<body>
    <h1>BloodBank Records</h1>
    <table border="1">
        <tr>
            <th>Blood Bank Name</th>
            <th>Address</th>
            <th>City</th>
            <th>Phone</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
        <tr th:each="bloodBank : ${bloodBanks}">
            <td th:text="${bloodBank.bloodbankName}"></td>
            <td th:text="${bloodBank.address}"></td>
            <td th:text="${bloodBank.city}"></td>
            <td th:text="${bloodBank.phone}"></td>
            <td th:text="${bloodBank.email}"></td>
            <td>
                <!-- Delete Button -->
                <form th:action="@{/bloodbanks/delete}" method="post" style="display:inline;">
                    <input type="hidden" name="bloodbankName" th:value="${bloodBank.bloodbankName}"/>
                    <input type="hidden" name="city" th:value="${bloodBank.city}"/>
                    <button type="submit">Delete</button>
                </form>

                <!-- Update Button -->
                <form th:action="@{/bloodbanks/update}" method="get" style="display:inline;">
                    <input type="hidden" name="bloodbankName" th:value="${bloodBank.bloodbankName}"/>
                    <input type="hidden" name="city" th:value="${bloodBank.city}"/>
                    <button type="submit">Update</button>
                </form>
            </td>
        </tr>
    </table>

    <!-- Return to Home button -->
    <br><br>
    <a href="/"><button>Return to Home</button></a>
</body>
</html>
